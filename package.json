{
  "name": "points",
  "version": "0.1.0",
  "description": "Projet en TS sur la POO avec des points en 2 ou 3 dimensions",
  "main": "./dist/index.js",
  "scripts": {
    "start": "tsc && node ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
